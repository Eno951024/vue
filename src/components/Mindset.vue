<script setup>
import { mindsetText } from "../constants";
</script>

<template>
    <section id="mindset">
        <div class="mindset_inner">
            <h2 class="mindset_title">
                Mindset
            </h2>
            <div class="mindset_desc">
                <div v-for="(mindset, key) in mindsetText" :key="key">
                    <span>{{ key+1 }}.</span>
                    <h3>{{ mindset.title }}</h3>
                    <p>{{ mindset.desc }}</p>
                </div>
            </div>
        </div>
    </section>
</template>

<style lang="scss">
.mindset_inner {
    padding: 16px;
    display: flex;
    justify-content: space-between;

    @media (max-width: 800px){ 
        flex-direction: column;
    }

    .mindset_title {
        position: sticky;
        top: 70px;
        left: 0;
        width: 48%;
        height: 5vw;
        font-size: 4vw;
        font-weight: 900;
        line-height: 1.6;
        font-family: var(--mainKor-font);
        text-transform: uppercase;
        color: var(--black100);
        border-bottom: 0.4vw solid var(--black100);

        @media (max-width: 800px){ 
            width: 100%;
            margin-bottom: 10vw;
            font-size: 30px;
            height: auto;
            top: 68px;
            background-color: var(--mainBg-color);
        }
    }
    .mindset_desc {
        width: 50%;

        @media (max-width: 800px){ 
            width: 100%;
        }

        span {
            font-size: 5vw;
            line-height: 1;
            font-weight: 900;
            line-height: 1.3;
            font-family: var(--mainNum-font);

            @media (max-width: 800px){ 
                font-size: 20vw;
            }
        }
        h3 {
            font-size: 1.5rem;
            text-decoration: underline;
            text-underline-position: under;
            margin-bottom: 1vw;
        }
        p {
            margin-bottom: 30vh;
            font-size: 1.1rem;
            @media (max-width: 800px){ 
                margin-bottom: 0;
            }
        }
    }
}
</style>